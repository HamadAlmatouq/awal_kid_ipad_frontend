name: awal_kid_ipad_frontend
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.2.19
  iconsax: ^0.0.8
  shared_preferences: ^2.0.13
  dio: ^4.0.6
  dart_jsonwebtoken: ^2.14.2
  qr_code_scanner: ^1.0.1
  go_router: ^14.6.2
  jwt_decode: ^0.3.1
  provider: ^6.1.2
  audioplayers: ^6.1.0
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/lemonade/
    - assets/audio/
